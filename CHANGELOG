2026-09-12--收尾补洞：对象已 available、证据仍 pending 时 cleanup 只 commit 指针，不标 worker_lost；方案 D3 / §10 与实现对齐
2026-09-12--复查整改 P6 证据补传与收尾：putObjectEvidence 在字节仍在调用栈时重试、对象已 available 只 commit 证据、reviewRun 与 cleanup 扫描闭环迁出证据轴；空 Trace 落 capture_failed 行，运行列表状态与证据分列
2026-09-12--落地 P6 Evidence / Trace / 授权下载：证据轴与执行结论分开、债务先登记后上传、授权下载与 Viewer、Trace 按 Attempt 分 chunk、写入前脱敏；S04 在受控站点测得 Trace always 约 +0.3s / 12KiB，默认仍 off
2026-09-12--写下 P6 其余部分的方案（Evidence / Trace / 授权下载）：evidenceStatus 与执行结论分两根轴、证据债务先登记后上传接上零调用方的重投入口、崩溃只补账不重新截图、API 第一次依赖对象存储需显式改依赖边表、Trace 按 Attempt 分 chunk 失败保留、脱敏靠 Step 声明与 Secret 值级替换而不猜字段名；D7 记下一处对路线图的收窄（截图默认仍是失败一张）
2026-09-12--收口两处红：FramePath 解析补上与候选定位同一套等待窗口（整条路径共用一个截止时间；iframe 已 attach 但 url 还是空串时不再判 SURFACE_LOST，L2 嵌套 iframe 40 次连跑不再偶发），容量腾位用例的假句柄补齐 waitFor / waitForFunction（漏方法不是类型错误，会被登录路径 catch 成 SESSION_AUTH_UNSUPPORTED）
2026-09-12--Browser Surface 复查修补：联调夹具入库、定位诊断与失败截图缺失原因写入证据、FrameStep.selector 真正匹配 iframe
2026-09-12--接入 SNC DPM 为 L3 只读目标系统（docs/targets，宪法只建索引），控制台编码 snc-dpm；Engine×真浏览器垂直切片进 CI 硬门槛；点击可开 popup 但后续步骤仍留原页；L3 已跑通自动登录+提取「数据库」与 WAITING_FOR_AUTH
2026-09-12--Browser Surface 落地：含浏览器步骤的 Run 在第一个 Attempt 之前经 BrowserPort acquire，占不到会话走 yieldPlacement；注册 navigate / click / fill / extract / assert，定位失败可判定，提交带 sessionLease，失败截一张；受控站点 tests/target-surface-lab 做 L2
2026-09-12--P4 后半回交接上进程内冷却：占不到会话走 yieldPlacement（库回交成功才记冷却），验收 10.2 不再手工传 excludeRunIds
2026-09-11--P4 后半落地：claimRun 按账号活会话做 Affinity、容量只数未过期租约；占不到会话先腾空闲位再回交并退避，配置错误拆 SESSION_TARGET_MISSING / SESSION_POLICY_INVALID，PROFILE_LOCKED 与失联不放键；运行详情派生 placement，S02 只对受控夹具限制采用
2026-09-11--控制台 RBAC 的 8 个 PATCH/PUT/DELETE 端点收成 POST（对齐 targets 的 `:id` 与 `:id/delete` 约定），补上宪法不变量 19 唯一的破口；检查读 Express 装配后的真实路由表而不是扫源码文本，@All 挂到具体业务路径同样会红，并已用重新塞回一个 @Patch 验证它确实变红
2026-09-11--RunLease 复查第二轮：优雅停机不再把在途 Run 洗成 CANCELLED（Engine 区分外部信号与库里说停，交回 RECOVERING 且断言必须真能被接管），恢复次数只数失败交接（优雅停机不计，否则滚动发布三次就把好 Run 推进待核查），NEEDS_REVIEW 不再写 finished_at，删掉无产生者的两个租约错误码，WORKER_ID_CONFLICT 启动前给可读原因；补 RF08 副作用孤儿/同事务回滚注入点/幂等/自愈/双开/容量与 L3 组件用例，运行页显示名称而非裸 UUID；进程维度的 kill/暂停/DB 断连与多进程 RF06 明确留成债务
2026-09-11--RunLease 三处致命修补：0011 收紧会话租约前先撤销存量 ACTIVE+NULL fencing（否则 23514 整份回滚、有会话的库永久升不上来）、心跳失败按 lost/instance_taken 分流（失联重新注册自愈，身份被接管退出进程，不再留不可观测的僵尸）、finishAttempt 加 barrierAfterVerify 让 D5 的提交边界真被测试卡住；三处均带负向验证
2026-09-11--复查修补 RunLease：Engine 接管后续跑 RUNNING 步骤、claimRun 要求 READY+instance、心跳失败自停；补接管/屏障/LOST/env 反例用例
2026-09-11--RunLease / Fencing / 恢复落地：Worker 领取带 fencing、过期与漂移同事务恢复、核查与确认目标系统登录；控制台只加场景/运行观察面，不把控制台账号、目标系统、浏览器会话混成一套
2026-09-11--控制台登录改为账号而非邮箱：默认管理员 admin / cairn-admin；已有库把 admin@cairn.dev 迁成 admin
2026-09-11--目标系统列表不再因历史编码含下划线而 500：出站 DTO 与创建 slug 规则拆开
2026-09-11--playwright 版本对齐：worker 与 web 统一到 1.63.0（同一 chromium 1243，装一次共用），分叉由 tools/check-deps.mjs 卡住；对齐后 worker 浏览器测试首次真正运行，修掉 loginWithCredentials 抛异常逃逸与用例共用会话键
2026-09-11--BrowserSession 处置出口：GET /browser-sessions 与 POST :id/dispose（session:read / session:dispose、0009 补种系统角色）、LOST 不再永久占键、worker reap 对账收掉已处置句柄、recordAudit 收敛为单份
2026-09-11--BrowserSession P1 收口：平台默认与 env 对齐、RF04 零 session 行、D8 复用单测、RF17 50 页、.env.example 去重
2026-09-11--BrowserSession 认证闭环补齐：WAITING_FOR_AUTH、认证超时、@cairn/secret 自动登录、LEASE_UNKNOWN、换代续租测、chromium 可选 runtime 测
2026-09-11--集成测试三处偶发收口：DROP DATABASE FORCE 的收尾 57P01 不再变成 unhandled error、db/api 补齐 hookTimeout（建库跑迁移实测 7s，默认 10s 越线）、supertest 先 listen 固定端口（每请求换临时端口会被别的进程复用，表现为本该存在的路由 404）；生产连接池同补 error 监听，空闲连接出错不再掀进程
2026-09-11--对象存储复查修订：仓根改惰性求值（容器与 s3 驱动不再被 pnpm-workspace.yaml 挡住启动）、purge 失败计数改单语句、缺失原因按错误码分流；SecretProvider 抽出 @cairn/secret，shared 回到无 node 依赖；web 的 React 类型钉死在自身 19.x，不再漏到扩展占住的 pnpm 提升槽
2026-09-11--BrowserSession / SessionLease 落地：会话纳管、三列状态、租约四操作、丢租即停、回收与重启自愈；Playwright 仅 worker
2026-09-10--对象存储内核落地：put/get/delete、本地与 S3 适配、保留清理；证据索引只挂对象指针
2026-09-10--执行内核复查修订：取消请求收口在 finishAttempt、停机与领取竞态、执行器异常分类、drain 终态兜底；shared 构建断点与四处 typecheck 转绿
2026-09-10--执行内核落地：场景版本与 Run 账本、Echo/Delay/Fail 引擎，无浏览器即可跑完并复盘
2026-09-10--目标系统新建可带首个账号与可选登录框定位，常见字段启发式进代码
2026-09-10--输入控件统一改回白底，规范与 Token 同步，禁止再铺浅灰填充
