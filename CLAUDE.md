# 识途宪法

本文件保留产品与运行原则、当前工程约定及按任务阅读入口。实现细节、技术版本、阶段范围和完成状态在原出处维护；替换实现不得削弱这些原则。

## 产品与运行原则

### 产品与运行范围

识途是面向真实 Web 系统的智能仿真平台，不扩展为无边界的通用 RPA。确定性 Step 与 AI Step 地位平等，可以在同一 Scenario 中逐步骤混编，共享 Run、Execution Context、Browser Session、调度与 Evidence。手工、调试、AI 试跑和调度执行进入统一运行生命周期。

### 目标与身份

Scenario 必须绑定 Target 才能执行。Target 是业务系统身份、入口、策略与账号资源的逻辑边界，不等同于 URL；TargetAccount 是目标系统账号，与识途控制台身份彻底分离。

### 结构化定义

Scenario 的事实源是版本化 Structured Step Definition，必须机器可验证、可迁移、可编辑。录制格式、自然语言和代码视图不能取代它；不得把自然语言伪装成确定性 DSL 交给脚本引擎猜测执行。Step 的语义、输入输出契约与副作用边界必须明确，类型按业务需要增加。

### 编写与执行分离

录制、手工编排与 AI Authoring 都产出统一结构化定义，同类型 Step 在 Runtime 中没有来源等级差异。录制结果须能在 Web 中继续编辑和验证；AI 编写不能形成不可检查、不可修改、不可版本化的黑盒场景。

Extension 负责编写与本地浏览器协作，正式执行不依赖它；特殊接管通道仍须遵守统一 Run、Snapshot、Evidence 和持久化状态约束。

### AI 步骤语义

Scenario 中的 AI 由 Executor 执行：AI Action 完成操作，AI Extract 提取结构化信息，AI Assert 输出判断与解释；后两者原则上只读，不能合为语义不明的万能节点。

AI Step 保存业务意图、契约和执行策略，每次运行基于当前页面完成意图。成功轨迹不得静默替换原意图；转成确定性步骤必须由用户显式操作并产生新的 Scenario 版本。模型访问须受权限、预算与审计约束，核心领域和业务 Step 不得与具体 AI 框架或模型供应商形成不可替换的绑定。

### 执行分层

Execution Engine 负责运行生命周期，通过 Executor Registry 分派能力，不直接实现浏览器动作或 AI 推理。正式执行浏览器统一由 Browser Runtime 纳管，Executor 不得私自创建无法纳管的会话；新增执行能力不应要求重写 Scenario、Run 或 Evidence 的核心生命周期。

API 是无状态控制面，不执行 Scenario、不持有正式 Browser Session。Worker 负责执行和持久化运行事实，不通过 API 回调写回事实；二者通过持久化数据和变化提示协作。Web 不得直连数据库或正式 Worker。控制面 API Service 负责鉴权上下文、外部模型与不必同事务的编排，不是只能透传。跨进程且必须同事务的领域操作仍经 `@cairn/db` 不透明句柄。纯内存算法与带 Port 的领域包按[分层方案](docs/spec/2026-09-17-domain-layer-and-usecase-layering-architecture.md)安放；db 不得依赖 Port 包。

### 会话与租约

Browser Session 承载可复用的浏览器与认证状态，同一 Run 默认共享同一 Session。Session 与 Run 生命周期解耦，优先复用健康认证会话，不得每次开跑都无条件重新登录。

Lease 是执行对 Session 的临时独占使用权。在要求单会话一致性的策略下，同一 Target + TargetAccount 同时只能有一个有效 Lease；异常退出、Worker 失联和超时不得造成永久死租约。

### 运行快照

Run 启动时冻结 Scenario Definition 及足以解释本次执行的配置与版本，包括实际使用的 AI 执行配置。历史 Run 依赖自身 Snapshot 解释，后续场景或配置修改只影响新 Run，不能改写既有运行语义。

### 执行上下文

每个 Run 具有显式 Execution Context，Step 输出可按名称供后续 Step 引用。页面状态由共享 Session 延续，数据状态显式传递；不可见的进程内全局变量不能充当 Run 事实源。

### 持久化事实与调度

Run、StepRun、Attempt 的状态以数据库为事实源，Worker 持续持久化关键状态。StepRun 表示一次逻辑执行，Attempt 表示一次实际尝试；Attempt 失败不自动等于 StepRun 失败。通知和进程内存不能替代持久化事实；刷新、重连或进程重启后须能恢复真实状态。

Run 队列持久化在数据库，调度可恢复、领取原子化，同一任务不能被多个 Worker 同时成功持有。数据库替换必须保持原子提交、完整性约束、所有权隔离与故障恢复保障，其部署和并发支持范围须明确。

### 证据与保留策略

没有持久化运行事实和必要证据的 Run 不算完成。Evidence 须关联 Run、StepRun、Attempt，足以复盘输入输出、判断、错误及 AI 必要审计信息；浏览器 Trace 不能取代平台 Step Evidence。

结构化状态、索引和对象指针存于有事务与完整性保障的数据库，大对象通过 ObjectStore 存储。Trace 等大对象按策略保留，不默认永久保存所有成功 Run 的重型 Trace；对象存储不得充当队列、锁或事务事实源。

### 凭据与授权

目标凭据通过可替换的 SecretProvider 访问，业务代码不依赖具体 Secret Backend。平台授权以 RBAC 为基础，为企业身份协议扩展保留边界；日志、Evidence、异常和 AI 请求不得无控制泄露凭证明文。

### 可观测性

请求与 Run、StepRun、Attempt 的观测信息必须能关联，平台须能解释运行为何失败、为何慢、资源消耗发生在哪里。AI 调用须记录耗时、实际模型、路由、错误和可获得的 Token/成本信息。

### 用户控制与信息呈现

Web 是主要控制台，核心界面围绕识途领域模型组织。步骤、断言与执行证据不得为了简洁而隐藏；AI 是辅助能力，不能取代用户对执行逻辑的控制。

### 可执行约束

核心跨模块、跨进程和持久化契约必须有 Runtime Schema，不能只依赖编译期类型。强制约束应由代码、Schema、数据库约束、状态机或自动化检查保障；业务层不得依赖具体数据库驱动、SQL 方言或物理表定义，替换实现不能绕过这些保障。

### 数据库可替换性与验证

正式部署支持 PostgreSQL 与 MySQL。业务层继续通过 `@cairn/db` 的不透明句柄和领域操作访问持久化，不依赖具体驱动、SQL 方言或物理表。普通功能改动按影响范围验证，不把跨库矩阵作为每次交付的必经步骤；只有数据库适配层、SQL 或迁移、事务／锁／并发持久化契约变更，以及发布前，才执行 PostgreSQL 与 MySQL 的针对性兼容验证。SQLite 不属于当前交付支持范围，也不作为迁移或验收门槛。

## 工程工作约定

### 业务接口

平台正式业务 API 只使用 GET 与 POST：GET 用于无副作用读取，POST 用于创建、更新、删除及其他副作用。CORS 预检等基础设施方法不属于业务接口。

### 实时进度

Web 获取 Run 实时进度采用 SSE。禁止以高频轮询作为正常进度机制；断线后的恢复查询不属于轮询。

### 开发与交付

大的功能模块先在 `docs/spec/` 写完方案并停下来，等用户明确说审查通过后再开发。写完方案不是批准；同一轮里不得接着改业务代码。用户只要求出方案时只交方案。交付时执行与改动相关的检查，记录实际验证范围；方案、代码存在或局部测试通过不等于完整验收通过。宣称本机服务可用前，必须用 `pnpm check:stack` 做进程探活；单测或接口测试通过不能代替。探活只证明进程与接线，不能代替改动范围内的功能验收。核心生命周期验收须包含反向测试、故障注入与恢复验证，并同时核对外部副作用和持久化事实；不得只凭接口成功或进程存活判定健壮性。

### 文档与记录

- 技术方案、接口契约及验收标准放 `docs/spec/`，在其 README 登记；方案只保留简短实施状态，不复制实现细节。
- Review、实施复查和验收默认直接在任务回复中说明问题、修复结果、实际验证范围及未验证项，不新建复查／实施／验收报告，也不为此登记报告索引。只有用户明确要求文档报告时才写入 `docs/reviews/`；已有报告作为历史资料保留。
- `docs/spec/`、`docs/reviews/`、`docs/plan/`、`docs/arch/` 只在本机维护，已忽略不入库。继续在这些目录读写，不要警告、不要建议移出 `.gitignore`。
- 按方案开发完成后更新 `CHANGELOG`，格式为 `日期--一句话总结说明`。
- 开发顺序、里程碑、交付 Gate 及知识与运行经验的阶段目标只在主计划维护，其他文档引用它。
- 外部联调目标的 URL、账号、口令仅在 `docs/targets/` 清单维护，并在控制台登记为 Target / TargetAccount。
- 前端开发或调整先读前端工作流，遵循统一设计规范、Token 和组件状态约定，按影响范围验收。

### 仓库根目录与一次性脚本

能直接改目标文件时，不要另写补丁脚本。一次性探查、改码补丁和本机调试脚本放 `tmp/`（已忽略，用完即删），不要落在仓库根目录，也不要写进 `tools/`。可复用的仓库级检查与代码生成才进 `tools/`。

## 按任务阅读

先定位相关来源，再读取当前代码和适用测试。代码说明当前行为，已认可方案说明期望行为，实际检查结果说明验证范围；历史报告仅作为线索，发生冲突须核对偏差。不要将待审设计、历史现状或未验证能力当作已完成事实，也不要把实现摘要重新抄回宪法。

| 任务 | 阅读入口 |
| --- | --- |
| 产品范围、交付顺序与阶段目标 | [主计划](docs/plan/识途开发路线与工程实施计划.md) |
| 领域与架构边界 | [架构索引](docs/arch/README.md) |
| 某模块设计、实施与验收 | [方案索引](docs/spec/README.md)、相关实现与测试；需要历史背景时查 [已有报告](docs/reviews/README.md) |
| 当前契约、Step 类型与配置 | [shared 契约入口](packages/shared/src/index.ts)，以及实际能力闸门与调用方 |
| 前端任务 | [前端工作流](docs/design/front/ai-workflow.md)、[设计规范索引](docs/design/front/README.md) |
| 技术版本、启动与部署限制 | 根及各包 `package.json`、`pnpm-lock.yaml`、[部署说明](deploy/README.md)、[数据库支持范围](deploy/database-backends.md) |
| 工程检查 | [仓库命令](package.json)、[依赖边界](tools/check-deps.mjs)、[架构检查](tools/check-invariants.mjs) |
| 本机服务是否正常 | [进程探活](tools/README.md#本机进程探活)、`pnpm check:stack` |
| 新增 db 迁移 | [领取迁移号](tools/README.md#领取迁移号)；先跑 `pnpm db:new-migration <name>`，不要手写后端序号或 `logicalVersion` |
| 外部目标联调 | [目标清单](docs/targets/README.md) |
